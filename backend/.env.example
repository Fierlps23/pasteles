# Ejemplo de variables de entorno para el backend
# Rellena con valores reales en tu panel (Render/Heroku/Vercel) y NUNCA subas secrets al repo.

# Conexión a la base de datos (opciones: variables individuales o URL completa)
# Opción A: variables por separado
DB_USER=root
DB_PASS=secret_password
DB_HOST=your-db-host.example.com
DB_PORT=3306
DB_NAME=pasteleria_db

# Opción B: URL completa (se intenta parsear si está presente)
# Ejemplo: mysql://user:pass@host:port/dbname
#MYSQL_PUBLIC_URL=mysql://root:password@shuttle.proxy.rlwy.net:36862/railway
#DATABASE_URL=mysql://root:password@shuttle.proxy.rlwy.net:36862/railway

# Mercado Pago (obligatorio para pagos)
MERCADOPAGO_ACCESS_TOKEN=APP_USR-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MERCADOPAGO_PUBLIC_KEY=APP_USR-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MERCADOPAGO_BACK_URL_BASE=https://tu-backend-publico.onrender.com

# Seguridad / Flask
SECRET_KEY=una_clave_larga_y_random

# Opcional: nombre del entorno
FLASK_ENV=production

# Nota:
#  - Define estas variables en el panel de Render (o en la configuración de tu proveedor) y no las subas al repositorio.
#  - El backend aceptará tanto las variables DB_* como varias variables compat (MYSQL*, DATABASE_URL). Si usas una URL completa, descomenta la línea correspondiente.
